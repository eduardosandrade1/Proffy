module.exports = async function(db, {proffyValue, classValue, classScheduleValue}){
    // inserir dados na tabela de teachers
    await db.run() // await aguarda aquela mesma linha terminar para rodar o restante do código
}